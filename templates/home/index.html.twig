{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}
{% block meta_description %}Colocation.com – Gérez votre colocation facilement. Loyers, charges, tantièmes, messagerie, planning ménager.{% endblock %}

{% block body %}

{# Hero Section #}
<section class="hero-section text-center">
    <div class="container">
        <h1 class="display-4 fw-bold mb-3"><i class="bi bi-house-heart-fill"></i> Colocation.com</h1>
        {% if is_granted('ROLE_PROPRIETAIRE') %}
            <p class="lead mb-4">Bienvenue, {{ app.user.prenom }}. Gérez vos colocations, suivez vos loyers et communiquez avec vos locataires.</p>
            <a href="{{ path('app_proprietaire_dashboard') }}" class="btn btn-light btn-lg me-3">
                <i class="bi bi-speedometer2"></i> Mon tableau de bord
            </a>
            <a href="{{ path('app_annonce_new') }}" class="btn btn-outline-light btn-lg">
                <i class="bi bi-plus-circle"></i> Publier une annonce
            </a>
        {% elseif is_granted('ROLE_USER') %}
            <p class="lead mb-4">Bienvenue, {{ app.user.prenom }}. Consultez vos loyers, téléchargez vos quittances et contactez votre propriétaire.</p>
            <a href="{{ path('app_locataire_dashboard') }}" class="btn btn-light btn-lg me-3">
                <i class="bi bi-speedometer2"></i> Mon espace locataire
            </a>
            <a href="{{ path('app_annonces') }}" class="btn btn-outline-light btn-lg">
                <i class="bi bi-search"></i> Voir les annonces
            </a>
        {% else %}
            <p class="lead mb-4">La plateforme qui simplifie la gestion de votre colocation.<br>Loyers, charges, messagerie et planning en un seul endroit.</p>
            <a href="{{ path('app_annonces') }}" class="btn btn-light btn-lg me-3">
                <i class="bi bi-search"></i> Voir les annonces
            </a>
            <a href="{{ path('app_register') }}" class="btn btn-outline-light btn-lg">
                <i class="bi bi-person-plus"></i> S'inscrire gratuitement
            </a>
        {% endif %}
    </div>
</section>

{# Stats #}
<section class="py-4 bg-white border-bottom">
    <div class="container">
        <div class="row text-center g-4">
            <div class="col-6 col-md-3">
                <div class="fw-bold display-6 text-primary">3 200+</div>
                <div class="text-muted small">colocataires inscrits</div>
            </div>
            <div class="col-6 col-md-3">
                <div class="fw-bold display-6 text-primary">850+</div>
                <div class="text-muted small">propriétaires</div>
            </div>
            <div class="col-6 col-md-3">
                <div class="fw-bold display-6 text-primary">12 000+</div>
                <div class="text-muted small">loyers gérés</div>
            </div>
            <div class="col-6 col-md-3">
                <div class="fw-bold display-6 text-primary">4,8 / 5</div>
                <div class="text-muted small">satisfaction moyenne</div>
            </div>
        </div>
    </div>
</section>

{# Propriétaires / Locataires #}
<section class="py-5">
    <div class="container">
        {% if is_granted('ROLE_PROPRIETAIRE') %}
        <h2 class="text-center fw-bold mb-5">Votre espace propriétaire</h2>
        <div class="row g-4 justify-content-center">
            <div class="col-md-8">
                <div class="card h-100 p-4 border-primary" style="border-width:2px !important;">
                    <div class="d-flex align-items-center mb-3">
                        <i class="bi bi-building text-primary display-5 me-3"></i>
                        <h3 class="fw-bold mb-0 h4">Ce que vous pouvez faire</h3>
                    </div>
                    <ul class="list-unstyled mb-4">
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Gérez toutes vos colocations depuis un seul tableau de bord</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Suivez les loyers et relancez les impayés en un clic</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Répartissez les charges automatiquement selon la surface</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Générez les quittances PDF sans effort</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Publiez des annonces et suivez les visites</li>
                    </ul>
                    <div class="d-flex gap-2 mt-auto">
                        <a href="{{ path('app_proprietaire_dashboard') }}" class="btn btn-primary">Mon tableau de bord</a>
                        <a href="{{ path('app_proprietaire_colocations') }}" class="btn btn-outline-primary">Mes colocations</a>
                    </div>
                </div>
            </div>
        </div>
        {% elseif is_granted('ROLE_USER') %}
        <h2 class="text-center fw-bold mb-5">Votre espace locataire</h2>
        <div class="row g-4 justify-content-center">
            <div class="col-md-8">
                <div class="card h-100 p-4 border-success" style="border-width:2px !important;">
                    <div class="d-flex align-items-center mb-3">
                        <i class="bi bi-house-door text-success display-5 me-3"></i>
                        <h3 class="fw-bold mb-0 h4">Ce que vous pouvez faire</h3>
                    </div>
                    <ul class="list-unstyled mb-4">
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Consultez vos loyers et leur statut à tout moment</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Téléchargez vos quittances en PDF pour vos démarches</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Visualisez votre part des charges mensuelles</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Organisez les tâches ménagères avec vos colocataires</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Contactez votre propriétaire via la messagerie intégrée</li>
                    </ul>
                    <div class="d-flex gap-2 mt-auto">
                        <a href="{{ path('app_locataire_dashboard') }}" class="btn btn-success">Mon espace</a>
                        <a href="{{ path('app_annonces') }}" class="btn btn-outline-success">Voir les annonces</a>
                    </div>
                </div>
            </div>
        </div>
        {% else %}
        <h2 class="text-center fw-bold mb-5">Une plateforme pour tout le monde</h2>
        <div class="row g-4">
            <div class="col-md-6">
                <div class="card h-100 p-4 border-primary" style="border-width:2px !important;">
                    <div class="d-flex align-items-center mb-3">
                        <i class="bi bi-building text-primary display-5 me-3"></i>
                        <h3 class="fw-bold mb-0 h4">Vous êtes propriétaire</h3>
                    </div>
                    <ul class="list-unstyled mb-4">
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Gérez toutes vos colocations depuis un seul tableau de bord</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Suivez les loyers et relancez les impayés en un clic</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Répartissez les charges automatiquement selon la surface</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Générez les quittances PDF sans effort</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Communiquez directement avec vos locataires</li>
                    </ul>
                    <a href="{{ path('app_register') }}" class="btn btn-primary mt-auto">Créer mon espace propriétaire</a>
                </div>
            </div>
            <div class="col-md-6">
                <div class="card h-100 p-4 border-success" style="border-width:2px !important;">
                    <div class="d-flex align-items-center mb-3">
                        <i class="bi bi-house-door text-success display-5 me-3"></i>
                        <h3 class="fw-bold mb-0 h4">Vous êtes locataire</h3>
                    </div>
                    <ul class="list-unstyled mb-4">
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Consultez vos loyers et leur statut à tout moment</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Téléchargez vos quittances en PDF pour vos démarches</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Visualisez votre part des charges mensuelles</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Organisez les tâches ménagères avec vos colocataires</li>
                        <li class="mb-2"><i class="bi bi-check-circle-fill text-success me-2"></i>Contactez votre propriétaire via la messagerie intégrée</li>
                    </ul>
                    <a href="{{ path('app_annonces') }}" class="btn btn-outline-primary mt-auto">Trouver une colocation</a>
                </div>
            </div>
        </div>
        {% endif %}
    </div>
</section>

{# Propriétaires bien notés #}
{% if proprietaires_notes|length > 0 %}
<section class="py-5 bg-white">
    <div class="container">
        <h2 class="text-center fw-bold mb-5"><i class="bi bi-star-fill text-warning me-2"></i>Propriétaires bien notés</h2>
        <div class="row g-4 justify-content-center">
            {% for p in proprietaires_notes %}
            <div class="col-md-4">
                <a href="{{ path('app_profil_proprietaire', {id: p.id}) }}" class="text-decoration-none text-dark">
                    <div class="card p-4 h-100 text-center">
                        {% if p.photo_profil %}
                            <img src="{{ asset('uploads/profils/' ~ p.photo_profil) }}"
                                 width="72" height="72" class="rounded-circle mx-auto mb-3"
                                 style="object-fit:cover;" loading="lazy">
                        {% else %}
                            <div class="rounded-circle bg-primary d-flex align-items-center justify-content-center mx-auto mb-3 text-white fw-bold"
                                 style="width:72px;height:72px;font-size:1.5rem;">
                                {{ p.prenom|slice(0,1)|upper }}
                            </div>
                        {% endif %}
                        <div class="fw-bold mb-1">{{ p.prenom }} {{ p.nom|slice(0,1) }}.</div>
                        <div class="text-muted small mb-2">Propriétaire</div>
                        <div class="mb-1">
                            {% for i in 1..5 %}
                                <i class="bi bi-star{{ i <= p.moyenne ? '-fill text-warning' : ' text-muted' }}"></i>
                            {% endfor %}
                        </div>
                        <div class="text-muted small">{{ p.moyenne }}/5 ({{ p.nb_avis }} avis)</div>
                    </div>
                </a>
            </div>
            {% endfor %}
        </div>
    </div>
</section>
{% endif %}

{# Annonces récentes #}
{% if annonces_recentes|length > 0 %}
<section class="py-5 bg-white">
    <div class="container">
        <h2 class="fw-bold mb-4">Annonces récentes</h2>
        <div class="row g-4">
            {% for annonce in annonces_recentes %}
            <div class="col-md-4">
                <div class="card h-100">
                    {% if annonce.premierPhoto %}
                        <img src="{{ asset('uploads/annonces/' ~ annonce.premierPhoto.filename) }}"
                             class="card-img-top" alt="{{ annonce.premierPhoto.alt }}"
                             style="height:200px;object-fit:cover;" loading="lazy">
                    {% else %}
                        <div class="card-img-top bg-secondary d-flex align-items-center justify-content-center" style="height:200px;">
                            <i class="bi bi-house text-white display-4"></i>
                        </div>
                    {% endif %}
                    <div class="card-body">
                        <h5 class="card-title">{{ annonce.titre }}</h5>
                        <p class="text-muted small"><i class="bi bi-geo-alt"></i> {{ annonce.localisation }}</p>
                        <p class="fw-bold text-primary fs-5">{{ annonce.prix }}€ / mois</p>
                    </div>
                    <div class="card-footer bg-transparent">
                        <a href="{{ path('app_annonce_show', {id: annonce.id}) }}" class="btn btn-primary w-100">
                            Voir le détail
                        </a>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
        <div class="text-center mt-4">
            <a href="{{ path('app_annonces') }}" class="btn btn-outline-primary">Toutes les annonces</a>
        </div>
    </div>
</section>
{% endif %}

{# FAQ #}
<section class="py-5" id="faq">
    <div class="container">
        <h2 class="fw-bold text-center mb-5">Questions fréquentes</h2>
        <div class="accordion" id="faqAccordion">
            {% if is_granted('ROLE_PROPRIETAIRE') %}
            {% set faqs = [
                {q: 'Comment publier une annonce ?', a: 'Depuis votre tableau de bord, cliquez sur "Annonces" puis "Nouvelle annonce". Renseignez le titre, la description, le prix et ajoutez des photos. L\'annonce sera visible immédiatement sur la plateforme.'},
                {q: 'Comment accepter ou refuser une candidature ?', a: 'Dans le menu "Candidatures", vous voyez toutes les candidatures reçues avec les pièces justificatives. Cliquez sur "Accepter" pour choisir la chambre à attribuer au locataire, ou sur "Refuser" pour lui envoyer un message automatique de refus.'},
                {q: 'Comment gérer les loyers impayés ?', a: 'Dans la rubrique "Loyers", les loyers en retard apparaissent en rouge. Vous pouvez contacter le locataire concerné via la messagerie intégrée ou marquer manuellement un loyer comme payé depuis son détail.'},
                {q: 'Comment répartir les charges entre les colocataires ?', a: 'Créez une charge (électricité, internet...) depuis la rubrique "Charges". Les tantièmes sont calculés automatiquement selon la surface de chaque chambre. Chaque locataire voit sa part depuis son espace.'},
                {q: 'Comment générer une quittance de loyer ?', a: 'Lorsqu\'un loyer est marqué comme payé, la quittance est générée automatiquement. Votre locataire peut la télécharger depuis son espace. Vous pouvez aussi la générer manuellement depuis la liste des loyers.'},
                {q: 'Comment évaluer un locataire ?', a: 'Dans la rubrique "Evaluations", cliquez sur "Evaluer" en face du locataire concerné. Attribuez une note de 1 à 5 et ajoutez un commentaire. Cette évaluation sera visible sur le profil public du locataire.'}
            ] %}
            {% elseif is_granted('ROLE_USER') %}
            {% set faqs = [
                {q: 'Comment candidater à une annonce ?', a: 'Sur la page d\'une annonce disponible, cliquez sur "Candidater". Vous devrez joindre une pièce d\'identité et un justificatif de revenus (PDF ou image, 5 Mo max). Le propriétaire recevra votre dossier et vous répondra via la messagerie.'},
                {q: 'Comment contacter mon propriétaire ?', a: 'Rendez-vous dans la rubrique "Messagerie" de votre espace locataire. Vous pouvez y envoyer des messages et consulter l\'historique complet de votre échange. Appuyez sur Entrée pour envoyer (Shift+Entrée pour un saut de ligne).'},
                {q: 'Comment obtenir ma quittance de loyer ?', a: 'Dans la rubrique "Quittances", vous trouverez toutes vos quittances disponibles. Cliquez sur "Télécharger PDF" pour obtenir votre justificatif, utile pour vos dossiers APL ou de relogement.'},
                {q: 'Mon loyer est marqué impayé alors que je l\'ai réglé, que faire ?', a: 'Si votre paiement n\'est pas reflété, utilisez le bouton "Payer" depuis votre rubrique "Loyers" pour simuler le paiement en ligne. Une quittance sera générée automatiquement et votre propriétaire sera notifié.'},
                {q: 'Comment résilier mon bail ?', a: 'Dans votre espace locataire, cliquez sur "Résilier mon bail" (accessible depuis le tableau de bord). Confirmez l\'action et votre propriétaire en sera informé automatiquement. Vos annonces de colocation repasseront en disponible.'},
                {q: 'Comment évaluer mon propriétaire ?', a: 'Dans la rubrique "Evaluations", cliquez sur "Evaluer mon propriétaire" si vous avez payé au moins un loyer. Attribuez une note et un commentaire. Vous pouvez aussi accéder à son profil public depuis une annonce pour laisser votre avis.'}
            ] %}
            {% else %}
            {% set faqs = [
                {q: 'Est-ce gratuit de s\'inscrire ?', a: 'Oui, l\'inscription est entièrement gratuite pour les locataires comme pour les propriétaires. Créez votre compte en quelques minutes et accédez à toutes les fonctionnalités sans engagement.'},
                {q: 'Quelle est la différence entre un compte locataire et propriétaire ?', a: 'Un compte locataire vous permet de candidater à des annonces, consulter vos loyers et quittances, et contacter votre propriétaire. Un compte propriétaire vous permet de gérer vos colocations, publier des annonces, suivre les loyers et traiter les candidatures.'},
                {q: 'Comment trouver une colocation ?', a: 'Rendez-vous dans la rubrique "Annonces". Vous pouvez filtrer par ville et par budget. Cliquez sur une annonce pour voir les détails et le profil du propriétaire, puis candidatez directement en ligne.'},
                {q: 'Mes données personnelles sont-elles sécurisées ?', a: 'Oui. Les mots de passe sont chiffrés et jamais stockés en clair. Les pièces jointes (candidatures) ne sont accessibles qu\'aux parties concernées. Toutes les communications transitent de manière sécurisée.'},
                {q: 'Puis-je avoir plusieurs colocataires dans le même logement ?', a: 'Oui, la plateforme est conçue pour la colocation. Chaque locataire a son propre espace personnel et les charges sont automatiquement réparties entre les chambres selon leur surface.'},
                {q: 'Comment signaler un problème dans le logement ?', a: 'Utilisez la messagerie intégrée pour informer votre propriétaire (fuite, panne...). Vos messages sont horodatés, ce qui constitue une trace écrite utile en cas de litige.'}
            ] %}
            {% endif %}
            {% for i, qa in faqs %}
            <div class="accordion-item">
                <h3 class="accordion-header">
                    <button class="accordion-button {{ i > 0 ? 'collapsed' : '' }}" type="button"
                            data-bs-toggle="collapse" data-bs-target="#faq{{ i }}">
                        {{ qa.q }}
                    </button>
                </h3>
                <div id="faq{{ i }}" class="accordion-collapse collapse {{ i == 0 ? 'show' : '' }}" data-bs-parent="#faqAccordion">
                    <div class="accordion-body">{{ qa.a }}</div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

{% endblock %}
